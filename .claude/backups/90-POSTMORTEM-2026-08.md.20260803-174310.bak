# 90 — 覆盤：2026-07-26 峰值後 -16.4% 回撤

> 資料來源：線上 runner `/snapshot`（2026-08-03T11:56Z，271 筆已平倉交易，`stateVersion` 26724）。
> 分析腳本邏輯記於本文各節；結論以 R 倍數為準，$ 金額會被權益成長污染。

## 0. 現況

| 項目 | 值 |
|---|---|
| 初始 / 現在 / 峰值權益 | 1000 / 1223.46 / 1464.17 |
| 峰值後回撤 | **-16.4%** |
| 總交易 | 271 筆（2026-06-26 ~ 08-03） |
| runner 狀態 | `running: false`，1 筆未平倉（ZECUSDT，56h） |
| 連敗計數 | 3 |

## 1. 結論先講

**這不是「策略失效」，是「策略的獲利來源被自己的出場規則切掉了」。**

> **補充（後續分析，見 §3.5）**：出場退化是真的，但**單就 era C 的虧損金額而言，更大的一塊在進場端**——
> -11.4R 裡有 -9.8R（86%）來自「同一標的的第 4 次以後進場」。兩者不衝突：
> 出場規則砍掉了尾巴（讓期望值失去唯一來源），重複進場則持續產生穩定的負期望值筆數。

勝率幾乎沒變（41% → 39%），平均虧損也幾乎沒變（-0.86R → -0.92R）。
唯一崩掉的是**右尾**：

| 期間 | 筆數 | 勝率 | 平均獲利 R | 平均虧損 R | 期望 R | R≥3 筆數 |
|---|---|---|---|---|---|---|
| A：momentum_exit 上線前（<07-22） | 173 | 40% | — | — | **+0.177** | 14（8.1%） |
| B：momentum_exit + giveback（07-22~07-28） | 46 | 46% | — | — | +0.109 | 3（6.5%） |
| C：低ATR 動能出場擴大後（≥07-28） | 52 | 38% | — | — | **-0.220** | **0** |

era C 的 52 筆交易裡，**R≥3 的交易是 0 筆**。

## 2. 為什麼「0 筆大贏」＝ 全盤皆輸

這套策略從頭到尾都是**極度依賴肥尾**的系統。用 A 期資料量化：

```
R≥3 的交易佔 8.1% 的筆數，卻貢獻 0.342R/筆，
而整個 A 期的期望值只有 0.177R/筆 → 大贏交易貢獻了 193% 的期望值。
```

換句話說：**把最大的那幾筆拿掉，這套策略是負期望值的。**

峰值前 204 筆的另一種切法，更觸目驚心：

| 取前 N 大交易 | 貢獻 | 其餘交易合計 |
|---|---|---|
| 前 1 筆（EULUSDT +$166, 13.2R） | $166 / 37% | 其餘 203 筆 = $289 |
| 前 3 筆 | $302 / 66% | 其餘 201 筆 = $153 |
| **前 5 筆** | **$413 / 91%** | **其餘 199 筆 = $43** |
| 前 10 筆 | $624 / 137% | 其餘 194 筆 = **-$169** |

**199 筆交易只賺 $43。所有的錢都是 5 筆交易賺的。**

所以「虧麻了」的正確描述不是「最近很爛」，而是**「最近沒中樂透，而這套系統的收入 100% 來自中樂透」**。
7/26 的權益峰值本身就是單筆 EULUSDT +13.2R 打上去的——用那個點當基準衡量回撤，本來就會過度悲觀。

## 3. 但確實有一個真實的、程式造成的退化

單純「運氣不好」解釋不完。蒙地卡羅（以 A 期 R 分布重抽 52 筆，20 萬次）：

```
era C 實際 = -11.4R
P(這麼慘或更慘 | 邊際未改變) = 3.05%
P(52 筆抽不到任何一筆 R≥3) = 1.2%
```

3% 不算「不可能」，但配合下面的機制證據，判定為**運氣 + 真實退化各半**。

### 3.1 主嫌：`momentum_exit` 把所有潛在跑單砍在 1R

`worker/src/index.js:1259`：

```js
if (p.bePartialDone && Number(p.barsSinceExtreme || 0) >= (
      Number(p.atrPct || 0) >= state.cfg.momentumMinAtr
        ? state.cfg.momentumStallBars       // 8
        : state.cfg.momentumLowAtrStallBars // 16
    )) {
  closePosition(state, p, close, 'momentum_exit', kTime(bar));
```

K 線是 **15m**。所以：

- `momentumStallBars: 8` = **收盤價 2 小時沒創新高就平倉**
- 低 ATR 標的 `16` = 4 小時
- 觸發前提 `bePartialDone` = 這筆已經走到 +8% 才會被納入判定

**它精準地只砍那些已經證明自己會動的單。** 而加密動能跑單在 72 小時的持有期內盤整 2 小時是常態，不是異常。

出場理由結構的變化（佔全部交易比例 / 平均 R / 該類總損益）：

| 出場理由 | A 期 | B 期 | C 期 |
|---|---|---|---|
| `trail_stop`（尾巴來源） | **11% / R2.98 / +$586** | 4% / R8.35 / +$209 | **4% / R1.69 / +$46** |
| `time_exit`（第二大來源） | **9% / R1.85 / +$310** | **0%（絕跡）** | **0%（絕跡）** |
| `lock_stop` | 8% / R1.52 / +$234 | 4% / R0.68 / +$17 | 4% / R0.58 / +$15 |
| `momentum_exit` | — | 13% / R1.30 / +$107 | **21% / R1.01 / +$150** |
| `stop` | 49% / R-1.00 | 52% / R-1.00 | 52% / R-1.00 |

看最後兩欄的算術：
**`trail_stop`(11%) + `time_exit`(9%) = 20%，幾乎正好等於 C 期 `momentum_exit` 的 21%。**
`momentum_exit` 吃掉的就是原本產生尾巴的那 20% 交易，然後用 **1.01R 取代原本 ~2.4R 的混合報酬**。

決定性佐證——**贏家的持有時間被砍半**：

| 期間 | 全體平均持有 | **贏家平均持有** | 贏家最長 |
|---|---|---|---|
| A | 19.1h | **32.6h** | 72h |
| B | 16.1h | 15.9h | 71h |
| C | 15.5h | **14.6h** | 66h |

A 期的贏家平均抱 32.6 小時；C 期只抱 14.6 小時。
`time_exit`（72h 上限）之所以絕跡，就是因為 `momentum_exit` 永遠會先觸發——沒有任何一筆單活得到 72 小時。

粗略反事實（上界，因為不是所有被砍的單都會變成跑單）：
C 期 11 筆 `momentum_exit` 若按 A 期 `trail_stop`+`time_exit` 的混合報酬（~2.4R）計算 → +15.3R，
足以把 C 期的 -11.4R 翻成正值。

### 3.2 次嫌：固定 % 目標 ÷ 浮動停損 = 報酬比被結構性壓縮

`buildRisk`（`worker/src/index.js:2649` / `momentum_trader_claude.html:1219`，兩檔完全一致）：

```js
const atrStopPct = Math.max(3, Math.min(8, 1.2 * atrValue));   // 停損 3~8%，結構停損可放寬到 10%
return { stop, tp1: entry*1.20, beTrigger: entry*1.08,
         lockTrigger: entry*1.15, lockLevel: entry*1.05, ... };
```

**分子（獲利目標）是固定百分比，分母（風險）是 3%~10% 浮動。**
所以同一個 `lockLevel: +5%`：

- 停損 3% → 兌現 **1.67R**
- 停損 10% → 兌現 **0.50R**

實測完全吻合：`lock_stop` 平均 R 從 A 期 **1.52** 掉到 C 期 **0.58**；`be_stop` 從 0.81 掉到 0.26。

依停損寬度分桶（全部 271 筆）：

| 停損寬度 | 筆數 | 勝率 | 期望 R | 平均獲利 R | 淨損益 |
|---|---|---|---|---|---|
| 0–5% | 161 | 35% | **+0.203** | 2.19 | **+$339** |
| 5–7% | 31 | 32% | -0.357 | 0.77 | -$118 |
| 7–9% | 20 | 45% | +0.031 | 1.19 | $1 |
| 9–11% | 55 | **62%** | +0.038 | **0.65** | **+$1** |

**9–11% 那一列是整份覆盤最該記住的一行：勝率 62%（全場最高），55 筆交易淨賺 $1。**
贏得更頻繁，但每次贏的錢不夠付一次虧損。

而寬停損部位的佔比正在上升：停損 ≥9.5% 的交易佔比 **A 期 14% → C 期 29%，翻倍**。

### 3.3 附帶確認：`volume_ignition` 回吐出場

7/24 的 `a8e38d8`（commit message 自己寫明「user-chosen despite harness regression」）：

| 期間 | 筆數 | 期望 R | 最大 R |
|---|---|---|---|
| A | 38 | +0.24 | 7.3 |
| B | 7 | +1.44 | 13.2 |
| C | 4 | **-0.54** | 0.8 |

樣本太小（C 期 4 筆）**不足以定罪**，但方向與「砍尾巴」一致：這個策略同時貢獻了史上最大單筆（EULUSDT 13.2R）與史上最大的尾巴風險。在 +4~8% 就強制了結，等於把它唯一的價值來源關掉。

## 3.5 ⚠️ 主嫌其實在進場端：同一標的的第 4 次以後進場

> 使用者提問「我怎麼感覺一直重複掃到一些會賠錢的訊號」後補做的分析。
> **結論：這個效應比 3.1 的出場退化更大——era C 的虧損有 86% 來自這裡。**

271 筆交易只有 **114 個不同標的**，82% 的交易發生在重複標的上。依「該標的第幾次進場」拆：

| 進場序 | 筆數 | 期望 R | 損益 | 最大 R |
|---|---|---|---|---|
| #1 | 114 | +0.158 | +$175 | 6.8 |
| #2 | 65 | +0.179 | +$153 | **13.2** |
| #3 | 38 | **+0.350** | +$132 | 7.3 |
| #4 | 23 | -0.272 | -$73 | 2.1 |
| #5+ | 31 | **-0.402** | -$164 | 3.5 |

**斷點乾淨地落在 3 和 4 之間。** 合計：#1-3 是 217 筆 / +$460 / +0.198R，#4+ 是 54 筆 / **-$237** / -0.346R。

### 各期虧損分解

| 期間 | 總計 | #1-3 | #4+ |
|---|---|---|---|
| A | +30.7R | +40.6R (n=155) | -9.9R (n=18) |
| B | +5.0R | +4.0R (n=29) | +1.0R (n=17) |
| C | **-11.4R** | -1.6R (n=33) | **-9.8R (n=19)** |

**era C 的 -11.4R 裡有 -9.8R（86%）來自 #4+；#1-3 幾乎打平（-1.6R）。**
跨期一致性是最強證據：**A 期 #4+ 是 -0.552R、C 期是 -0.515R——兩個獨立時期都一樣爛。**

### 完美連敗名單（≥3 次進場、無一獲利）

```
NBIS      4x  -1.0 -1.0 -1.0 -1.0    -$48
CAPUSDT   4x  -1.0 -1.0 -1.0 -1.0    -$48
SLXUSDT   4x  -1.0 -1.0 -1.0 -1.0    -$39
SMSN      3x  -1.0 -1.0 -1.0         -$36
SKHX      3x  -1.0 -0.0 -1.0         -$21
RAVEUSDT  3x  -1.0 -1.0 -0.1         -$21
                             合計 -$213
```

### 為什麼現行機制擋不住

`worker/src/index.js:1069` 的冷卻是「停損後 24 小時」，**會重置、無累積記憶**：

```js
.filter((t) => ['stop','be_stop','early_stop'].includes(t.reason)
            && Date.now() - t.exitTime < cfg.symbolStopCooldownMs)  // 24h
```

冷卻**本身有在運作**——「停損後 24h 內重新進場」實測 **0 次**。問題是等滿 24h 後，第 4 次進場與第 1 次待遇完全相同。

**分數也分辨不出來**：#1-3 平均分 **93.6**，#4+ 平均分 **93.3**。掃描器對「第 4 次的 NBIS」與全新強勢幣給一樣的分（呼應 2026-07-29 的「分數只能當門檻」）。

### ⚠️ 不能因此禁止重複進場——尾巴住在 #1-3 裡

史上最大單筆 EULUSDT **+13.2R 是第 2 次進場**，BASEDUSDT 的 6.8R / 7.3R 是第 1、3 次。
`BANKUSDT` 的 +3.5R 出現在第 6 次、`KAITOUSDT` 的 +2.1R 在第 4 次——**終身硬上限會誤殺這些**。

### 虛無分布檢定（依 2026-07-29 LESSONS 規定）

```
baseline                    24.2R
上限 3 次/標的              43.0R
隨機移除同樣 54 筆          中位 20.0R，5-95% [0.6, 36.3]
→ 百分位 99.3%   ✅ 通過
```

參數掃描形狀（cap 2→29.6 / **3→43.0** / 4→36.7 / 5→31.3 / ∞→24.2）為**單峰非孤立尖點**。
「連 k 敗後封鎖」的曲線更平滑且單調：連 2 敗封 3d→27.7 / 7d→28.9 / 14d→33.1 / 永久→35.7。

**該打的折（重要）**：
1. 規則從本資料挖出、又在本資料驗證（in-sample）。
2. 約 15 個變體的多重比較，最佳點 3 不可信，只有「某個上限有幫助」這個方向可信。
3. **這是原始加總、不是組合回放**——砍掉的倉位額度會被其他訊號填補（2026-07-29 記錄的排擠效應），實際效果未知。
→ 定案前必須過 `scripts/backtest-strategies.mjs`。**（已補做，見下節。）**

### ✅ 組合回放（2026-08-03 補做，OKX harness）

窗口：2026-07-04 ~ 08-03，OKX 前 25 大量能永續，15m，235 筆候選。**與上面的實盤資料完全不同的 universe 與時間窗**，因此這是真正的 out-of-sample 檢驗，不是 in-sample 自我驗證。

**先注意 baseline 本身**：這個窗口現行程式碼是 **portfolio -9.9 / PF 0.98 / 候選層 +23.0R**——幾乎打平。（70-STRATEGY-PLAYBOOK 表中 159.5 那組是舊窗口，已不代表現況。）

參數掃描（候選層 totR 為主訊號、確定性；portfolio 為組合回放）：

| 規則 | 移除 | 候選 totR | 候選 avgR | portfolio PnL | PF | 回撤 |
|---|---|---|---|---|---|---|
| baseline | 0 | 23.0 | 0.098 | -9.9 | 0.98 | 8.7% |
| 連 2 敗封 1d | 0 | — | — | — | — | — |
| 連 2 敗封 2d | 33 | 19.9 | 0.099 | -12.7 | 0.98 | 10.2% |
| 連 2 敗封 3d | 42 | 29.3 | 0.152 | +122.8 | 1.21 | 8.5% |
| **連 2 敗封 7d** | **71** | **29.8** | **0.182** | **+217.3** | **1.43** | **8.5%** |
| 連 2 敗永久 | 117 | 10.8 | 0.092 | +139.1 | 1.37 | 8.5% |
| 連 3 敗封 7d | 52 | 2.0 | 0.011 | +5.0 | 1.01 | 8.7% |
| 連 3 敗永久 | 76 | **-4.2** | -0.026 | +29.0 | 1.06 | 8.7% |
| 連 4 敗封 7d | 23 | 14.6 | 0.069 | +10.1 | 1.02 | 8.7% |

虛無分布（隨機移除同筆數，2000 次）：

| 規則 | 候選 totR | 虛無中位 | 虛無 5–95% | **百分位** |
|---|---|---|---|---|
| 連 2 敗封 2d | 19.9 | 20.5 | [6.4, 32.3] | 46.7%（＝亂砍） |
| 連 2 敗封 3d | 29.3 | 19.7 | [4.4, 32.9] | 88.4% |
| **連 2 敗封 7d** | **29.8** | 16.5 | [-0.7, 32.7] | **90.4%**（PnL 軸 93.6%） |
| 連 2 敗永久 | 10.8 | 11.7 | [-5.8, 29.9] | 46.9%（＝亂砍） |
| 連 3 敗永久 | -4.2 | 15.7 | [-1.6, 32.7] | **2.5%（比亂砍還差）** |

忠實版本（把規則 patch 進 `applyPortfolio`，streak 只由**實際成交**的部位計算，而非候選）：
**portfolio +133.2 / PF 1.24 / 回撤 8.5% / 勝率 41.0→45.6%**；同種子配對子抽樣 200 次 **66.5% 勝出、中位差 +52.6**（5–95% 差 [-101.7, +220.6]）。

**四個結論**：

1. **§3.5 推薦的形式（連 k 敗後暫時封鎖）在完全不同的樣本上重現了**，而且方向、斷點都對得上——這比 in-sample 的 99.3% 有力得多。
2. **§3.5 的字面形式（終身上限）沒過**：連 2 敗永久 46.9%（＝亂砍）、連 3 敗永久 2.5%（比亂砍差）。**這正是 §3.5 自己警告過的「終身硬上限會誤殺尾巴」，現在有回測證據了。**
3. **k=2 才是斷點，不是 k=3**。實盤序數看起來斷在 3/4 之間，但機制版本在 OKX 上 k=3、k=4 全部退化到雜訊內。
4. **D 至少要 3 天**：D=1 掃出 0 筆（同標的重複進場本來間隔就 >1 天，1 天封鎖是死碼）、D=2 落在 46.7%（亂砍）。

**仍要打的折**：
- 90.4% / 93.6% **沒到 95%**，依 2026-07-29 的標準這是「有訊號、未定案」，不是通過。
- 配對差的 5–95% 區間 [-101.7, +220.6] **跨越 0**，66.5% 勝率只是方向性。
- 15 格掃描的多重比較仍在。k=2 那條線 D=2 低於 baseline、D=3/7 高於——**非單調＝雜訊確實存在**；可信的是「暫時封鎖有幫助、永久封鎖有害」這個形狀，不是 D=7 這個值。
- universe 差異：harness 是 OKX 主流幣，實盤主力是 gate/xyz 小幣。**機制重現了，參數值不該直接搬。**
- 未計手續費與資金費率。

### 附帶佐證：強化了 3.1 的結論

**「`momentum_exit` 出場後、重新進場同一標的」→ 8 筆，期望 +2.19R，+$218。**
系統砍掉部位、又買回同一個幣，賺了 2.19R——**代表 `momentum_exit` 觸發時那波行情根本沒走完**。樣本小，但方向與「贏家持有時間砍半」完全吻合。

（對照組：「`stop` 出場後重新進場」71 筆、-0.190R、-$190。）

## 4. 各策略 / 各維度（峰值後 67 筆）

| 策略 | 筆數 | 損益 | 勝率 | 期望 R |
|---|---|---|---|---|
| `high_range_continuation` | 14 | **-$115** | 29% | -0.59 |
| `volume_ignition` | 7 | -$71 | 14% | -0.74 |
| `strong_momentum_breakout` | 6 | -$62 | 17% | -0.77 |
| `rally_downtrend`（唯一做空） | 11 | -$61 | 36% | -0.42 |
| `narrative_momentum` | 13 | -$20 | 38% | -0.09 |
| `volume_breakout_follow` | 3 | +$24 | 67% | +0.53 |
| `pullback_uptrend` | 5 | +$27 | 60% | +0.40 |
| `impulse_pullback_reclaim` | 8 | **+$46** | 75% | +0.42 |

- 做空（`rally_downtrend`）11 筆 -$61、期望 -0.42R：樣本小，但沒有一筆撐得住。
- 市場別：gate 53 筆 -$193、xyz 14 筆 -$39。gate 是主要失血處，但那也是主要交易場，無法單獨歸因。
- `impulse_pullback_reclaim` 與 `pullback_uptrend` 是 C 期僅存的正期望策略——**共同點是「回檔進場」，停損天生較窄**，正好呼應 3.2 的分桶結論。

## 5. 建議（依證據強度排序，**未實作、未部署**）

> 硬規則 4：策略參數屬使用者交易決策，本文只提供證據與選項。

0. **【最高優先】針對同一標的的重複進場設限**（見 §3.5）——證據最強、影響最大（era C 虧損的 86%），
   且**已補過 OKX harness 的組合回放並在 out-of-sample 重現**（見 §3.5 末節）。
   **具體形式：同一標的連 2 敗 → 封鎖 7 天**（不是終身上限、不是連 3 敗）。
   - 候選層 23.0R → 29.8R、avgR 0.098 → 0.182，虛無分布百分位 90.4%（PnL 軸 93.6%）
   - 忠實組合回放 -9.9 → **+133.2 / PF 0.98→1.24 / 回撤 8.5%**，配對子抽樣 66.5% 勝出
   - ⚠️ 90.4% **未達 95% 門檻**，配對差區間跨 0——這是「可以上線觀察的候選」，不是「已驗證的 edge」
   實作點：`eligibleEntrySignals`（`worker/src/index.js:1069`）加入 per-symbol 連敗計數與封鎖時間戳，
   HTML 側 `applyPortfolio` 需同步（雙實作）。建議做成 `cfg` 參數（`repeatLossBlockCount` / `repeatLossBlockDays`）
   以便 dashboard 即時調整與回退，設 0 即關閉。
1. **把 `momentumStallBars` 從 8 放寬**（8 根 15m = 2h 實在太緊）。
   最小改動、證據最強、可用 `scripts/backtest-strategies.mjs --patch` 直接掃 8/12/16/24/32。
   注意這是 `cfg` 參數，dashboard 可調，不必改 code。
2. **讓獲利目標隨風險縮放，而不是固定 %。**
   把 `lockLevel`、`beTrigger`、`tp1` 從 `entry*1.05` 這種寫法改成 `entry*(1 + k*stopPct)`。
   這是根因修正，但屬非 trivial 改動：**HTML + worker × 多空 = 四處**，且會改變所有既有回測基準。
3. **考慮直接限制停損寬度**：分桶顯示 >5% 停損的交易整體不賺錢。
   把 `structStop` 的 10% 上限收到 ~6%，或對寬停損標的降低倉位（讓 R 一致但 $ 風險下降）。
4. **`momentum_exit` 改成「降檔」而非「出場」**：停滯時收緊移動停損，而不是市價平倉，保留尾巴的可能性。
5. **不要因為這波回撤就加更多「訊號品質」過濾器。** `LESSONS.md` 已記錄兩次負結果（BTC 大盤狀態過濾、進場端過濾）。
   勝率沒掉，分數也分不出好壞（#1-3 是 93.6 分、#4+ 是 93.3 分）——再堆一層指標門檻只會砍到尾巴。
   建議 0 不屬於這類：它不是「更嚴的訊號門檻」，而是**用該標的自身的交易歷史**當條件，是現有 24h 冷卻的自然延伸。

## 6. 方法論教訓（值得寫進 LESSONS）

- **不要用權益峰值當覆盤基準。** 峰值必然由單筆極端獲利造成，從那點量回撤有選擇偏誤。
  本文改用**部署日期切分**（A/B/C），才看出退化發生在 07-22 與 07-28 兩次出場規則變更，而不是 07-26 的峰值。
- **肥尾系統的短期績效不可讀。** 當 8% 的交易貢獻 193% 的期望值，52 筆的樣本對「策略是否還有效」幾乎零資訊量。
  要判斷退化，得看**機制指標**（出場理由結構、贏家持有時間、MFE 捕捉率），不能看損益。
- **覆盤要沿「標的維度」切一次，不是只看時間與策略維度。** 前三節全部按時間/策略切，完全沒看見 §3.5 的效應；
  一旦按「該標的第幾次進場」重排，斷點立刻浮現，而且跨兩個獨立時期一致（A 期 -0.552R、C 期 -0.515R）。
  以後覆盤的標準切法至少要有：時間 × 策略 × **同標的重複次數**。
- **「機制有在運作」不等於「機制夠用」。** 24h 停損冷卻實測攔截率 100%（0 次違規），看起來完全健康，
  但它沒有累積記憶，所以第 4 次進場與第 1 次待遇相同。驗證一個防護機制時，要問的是「它擋掉的是不是真正該擋的那個分布」。
- **「提高勝率」的改動要當成危險訊號。** `momentum_exit` 讓 B 期勝率上升到 46%（全場最高）——同時期望值正在下降。
  在肥尾系統裡，勝率上升往往代表你在提早了結贏家。

---
*分析日期：2026-08-03 ｜ 資料：線上 runner 271 筆已平倉交易 ｜ 未修改任何策略程式碼*
